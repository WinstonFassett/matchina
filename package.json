{
  "name": "matchina",
  "version": "0.0.0",
  "description": "",
  "repository": "WinstonFassett/matchina",
  "license": "MIT",
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs"
    }
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "unbuild && size-limit",
    "dev": "vitest dev",
    "play": "shx ls ./playground | xargs -I {} bun run ./playground/{}",
    "play:watch": "nodemon --watch src --watch playground -e js,mjs,cjs,json,ts,tsx --exec \"bun run play\"",
    "play:watch:file": "nodemon --watch src --watch playground -e js,mjs,cjs,json,ts,tsx --exec \"bun run playground/lifecycle.usage.ts\"",
    "knip": "knip",
    "lint": "eslint --cache --ext .ts,.js,.mjs,.cjs . && prettier -c src test",
    "lint:fix": "eslint --cache --ext .ts,.js,.mjs,.cjs . --fix && prettier -c src test -w",
    "prepack": "bun run build",
    "release": "bun test && changelogen --release && npm publish && git push --follow-tags",
    "test": "bun lint:fix && bun test:types && vitest run --coverage",
    "test:types": "tsc --noEmit --skipLibCheck",
    "size": "size-limit"
  },
  "devDependencies": {
    "@size-limit/preset-small-lib": "^10.0.1",
    "@types/node": "^20.8.2",
    "@types/react": "^18.2.31",
    "@vitest/coverage-v8": "^0.34.6",
    "changelogen": "^0.5.5",
    "eslint": "^8.51.0",
    "eslint-config-unjs": "^0.2.1",
    "knip": "^2.38.6",
    "prettier": "^3.0.3",
    "react": "^18.2.0",
    "shx": "^0.3.4",
    "size-limit": "^10.0.1",
    "typescript": "^5.2.2",
    "unbuild": "^2.0.0",
    "vitest": "^0.34.6"
  }
}
