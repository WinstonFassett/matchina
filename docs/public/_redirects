# Serve the site under /matchina/ on Netlify previews
# If a request is already under /matchina/, serve as-is
# Keep /matchina/* working by rewriting to root content
/matchina/*   /:splat   200

# Do not rewrite everything else; let root paths resolve normally
