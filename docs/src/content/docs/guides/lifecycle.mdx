---
title: Lifecycle
description: Machine transition lifecycle
---

# Transition Flow: Lifecycle Hooks

All machine types — [FactoryMachine](/matchina/guides/machines), [StateMachine](/matchina/guides/state-machine-interface), [StoreMachine](/matchina/reference/interfaces/storemachine/) — share the same generic [EventLifecycle](/matchina/reference/interfaces/eventlifecycle). The tree below traces a single call through `machine.send(type, ...params)`, optionally entered via a typed [api wrapper](/matchina/guides/machine-enhancers).



<table class="lifetree not-content">
  <thead>
    <tr><th>Hook</th><th>Description</th><th>Returns</th></tr>
  </thead>
  <tbody>
    <tr><td data-depth="0"><a href="#0-api-type-params-optional"><code>{'api[type](...params)'}</code></a><span class="row-tag">optional</span></td><td>Model may call <code>machine.send(type, ...params)</code>.</td><td><code>void</code></td></tr>
    <tr><td data-depth="0"><a href="#1-send"><code>{'send(type, ...params)'}</code></a></td><td>Resolve the event and transition</td><td><code>void</code></td></tr>
    <tr><td data-depth="1"><a href="#2-resolveexit"><code>resolveExit(ev)</code></a></td><td>Resolve the change event with its <code>to</code> state.<span class="halt-cond">return <code>undefined</code> to exit</span></td><td><code>{'E | undefined'}</code></td></tr>
    <tr><td data-depth="1"><a href="#3-transition"><code>transition(ev)</code></a></td><td>Run the transition lifecycle pipeline.</td><td><code>{'E | undefined'}</code></td></tr>
    <tr><td data-depth="2"><a href="#4-guard"><code>guard(ev)</code></a></td><td>Is the change allowed?<span class="halt-cond">return <code>false</code> to halt</span></td><td><code>boolean</code></td></tr>
    <tr><td data-depth="2"><a href="#5-handle"><code>handle(ev)</code></a></td><td>Map trigger event to change event.<span class="halt-cond">return <code>undefined</code> to halt</span></td><td><code>{'E | undefined'}</code></td></tr>
    <tr><td data-depth="2"><a href="#6-before"><code>before(ev)</code></a></td><td>Last chance to abort or rewrite.<span class="halt-cond">return <code>undefined</code> to halt</span></td><td><code>{'E | undefined'}</code></td></tr>
    <tr><td data-depth="2"><a href="#7-update"><code>update(ev)</code></a></td><td>Apply the new state.</td><td><code>void</code></td></tr>
    <tr><td data-depth="2"><a href="#8-effect"><code>effect(ev)</code></a></td><td>Run side effects.</td><td><code>void</code></td></tr>
    <tr><td data-depth="3"><a href="#9-leave"><code>leave(ev)</code></a></td><td>What happens as we exit the old state?</td><td><code>void</code></td></tr>
    <tr><td data-depth="3"><a href="#10-enter"><code>enter(ev)</code></a></td><td>What happens as we land in the new state?</td><td><code>void</code></td></tr>
    <tr><td data-depth="2"><a href="#11-notify"><code>notify(ev)</code></a></td><td>Tell subscribers what happened.</td><td><code>void</code></td></tr>
    <tr><td data-depth="2"><a href="#12-after"><code>after(ev)</code></a></td><td>Final hook — transition is done.</td><td><code>void</code></td></tr>
  </tbody>
</table>


## Step-by-step

### 0. `api[type](...params)` (optional)

A strongly-typed app surface over the machine. Matchina ships factories (see [Machine Enhancers](/matchina/guides/machine-enhancers)) for generating these wrappers — `api.checkout(item)` instead of `send("checkout", item)`. They delegate to `machine.send` and otherwise stay out of the lifecycle. Hooks remain the conventional way to extend a machine; wrapper APIs are an additional extension point when you want one.

### 1. `send`

Where the event enters the machine. `machine.send(type, ...params)` resolves the change event and starts the transition pipeline.

```ts
send(type, ...params) {
  const resolved = machine.resolveExit({ type, params, from: lastChange.to });
  if (resolved) machine.transition(resolved);
}
```

If a generated API method is present (e.g. `api.doSomething(a, b)`), it is the first frame and delegates to `send`.

### 2. `resolveExit`

Resolves the `to` state for the event `{ type, from, to? }` — looks up the transition handler for the current state and event type and returns the resolved `to` state.

- Return `null` or `undefined` to exit before the lifecycle pipeline runs.

### 3. `transition`

The lifecycle pipeline. Each step below runs in order against the resolved event; `guard`, `handle`, and `before` can each halt propagation.

### 4. `guard`

Is the transition allowed?

- If `false`, the pipeline exits.

### 5. `handle`

First opportunity to process or rewrite the event.

- If returns `undefined`, the pipeline exits.

### 6. `before`

Final hook before the state change is applied. Can still rewrite the event.

- If returns `undefined`, the pipeline exits.

### 7. `update`

Applies the new state.

### 8. `effect`

Runs side effects. By default, this calls `leave` then `enter`.

### 9. `leave`

Called as the previous state is exited.

### 10. `enter`

Called as the new state is entered.

### 11. `notify`

Notifies subscribers of the change.

### 12. `after`

Final hook after the transition completes.

## Early exits

`resolveExit` returning no match, `guard` returning `false`, or `handle`/`before` returning `undefined` will stop the flow. No further steps run.
