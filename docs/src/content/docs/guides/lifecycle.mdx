---
title: Lifecycle
description: Machine transition lifecycle
---

# Transition Flow: Lifecycle Hooks

All machine types implement the same generic [EventLifecycle](/matchina/reference/interfaces/eventlifecycle) and hook methods. The same patterns work across a [FactoryMachine](/matchina/guides/machines), a [StateMachine](/matchina/guides/state-machine-interface), or a [StoreMachine](/matchina/reference/interfaces/storemachine/). The tree below traces a single call to `machine.send(type, ...params)`. Hooks marked with `*` can halt propagation.

<table class="lifetree not-content">
  <thead>
    <tr><th>Hook</th><th>Description</th></tr>
  </thead>
  <tbody>
    <tr><td data-depth="0"><a href="#1-send"><code>send</code></a></td><td>Where does the event enter?</td></tr>
    <tr><td data-depth="1"><a href="#2-resolveexit"><code>resolveExit</code></a><span class="halt-mark">{'*'}</span></td><td>Which state should we move to? <span class="halt-cond">no match — exit</span></td></tr>
    <tr><td data-depth="1"><a href="#3-transition"><code>transition</code></a></td><td>Run the lifecycle pipeline.</td></tr>
    <tr><td data-depth="2"><a href="#4-guard"><code>guard</code></a><span class="halt-mark">{'*'}</span></td><td>Is the transition allowed? <span class="halt-cond">return <code>false</code> to halt</span></td></tr>
    <tr><td data-depth="2"><a href="#5-handle"><code>handle</code></a><span class="halt-mark">{'*'}</span></td><td>How should the event be processed? <span class="halt-cond">return <code>undefined</code> to halt</span></td></tr>
    <tr><td data-depth="2"><a href="#6-before"><code>before</code></a><span class="halt-mark">{'*'}</span></td><td>Last chance to abort or rewrite. <span class="halt-cond">return <code>undefined</code> to halt</span></td></tr>
    <tr><td data-depth="2"><a href="#7-update"><code>update</code></a></td><td>Apply the new state.</td></tr>
    <tr><td data-depth="2"><a href="#8-effect"><code>effect</code></a></td><td>Run side effects.</td></tr>
    <tr><td data-depth="3"><a href="#9-leave"><code>leave</code></a></td><td>What runs as we exit the old state?</td></tr>
    <tr><td data-depth="3"><a href="#10-enter"><code>enter</code></a></td><td>What runs as we land in the new state?</td></tr>
    <tr><td data-depth="2"><a href="#11-notify"><code>notify</code></a></td><td>Tell subscribers the change happened.</td></tr>
    <tr><td data-depth="2"><a href="#12-after"><code>after</code></a></td><td>Final hook — transition is done.</td></tr>
  </tbody>
</table>

<p class="lifecycle-legend"><span class="halt-mark">{'*'}</span> can halt propagation — remaining steps are skipped.</p>

## Step-by-step

### 1. `send`

Where the event enters the machine. `machine.send(type, ...params)` resolves the change event and starts the transition pipeline.

```ts
send(type, ...params) {
  const resolved = machine.resolveExit({ type, params, from: lastChange.to });
  if (resolved) machine.transition(resolved);
}
```

If a generated API method is present (e.g. `api.doSomething(a, b)`), it is the first frame and delegates to `send`.

### 2. `resolveExit`

Looks up the transition handler for the current state and event type and returns the change event (or `undefined`).

- If `undefined`, `send` exits before the lifecycle pipeline runs.

### 3. `transition`

The lifecycle pipeline. Each step below runs in order against the resolved event; `guard`, `handle`, and `before` can each halt propagation.

### 4. `guard`

Is the transition allowed?

- If `false`, the pipeline exits.

### 5. `handle`

First opportunity to process or rewrite the event.

- If returns `undefined`, the pipeline exits.

### 6. `before`

Final hook before the state change is applied. Can still rewrite the event.

- If returns `undefined`, the pipeline exits.

### 7. `update`

Applies the new state.

### 8. `effect`

Runs side effects. By default, this calls `leave` then `enter`.

### 9. `leave`

Called as the previous state is exited.

### 10. `enter`

Called as the new state is entered.

### 11. `notify`

Notifies subscribers of the change.

### 12. `after`

Final hook after the transition completes.

## Early exits

`resolveExit` returning no match, `guard` returning `false`, or `handle`/`before` returning `undefined` will stop the flow. No further steps run.
